import axios from "axios";
import { api } from "../../constants/api";

export const addToCart = async (id, dispatch, isAuth, navigate, token) => {};

// export const removeFromCart = (id, dispatch) => {
//   dispatch({ type: "REMOVE_FROOM_CART", payload: id });
// };
